package com.foodieapp.api.config;

import org.springframework.context.annotation.Configuration;

/**
 * Stub for future load balancing.
 * Currently each service has a single URL configured in application.properties.
 * For production, use Spring Cloud LoadBalancer or a proper service registry.
 */
@Configuration
public class LoadBalancerConfig {
}
