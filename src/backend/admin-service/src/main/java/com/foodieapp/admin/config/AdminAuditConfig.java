package com.foodieapp.admin.config;
import org.springframework.context.annotation.Configuration;
@Configuration
public class AdminAuditConfig {
    // Audit logging configuration - currently using direct DB writes via AdminAuditService
}
